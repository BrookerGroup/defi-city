{
  "name": "deficity-subgraph",
  "version": "1.0.0",
  "description": "DefiCity - DeFi City Builder Game Subgraph",
  "repository": "https://github.com/your-org/deficity-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ deficity-subgraph",
    "create-local": "graph create --node http://localhost:8020/ deficity-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ deficity-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 deficity-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
